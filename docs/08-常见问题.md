# 08 常见问题

## 适用范围

- 出现白屏、路由失败、接口异常、端差异问题时使用。

## 规则

- 排查顺序固定：配置 -> 路由 -> 请求 -> 状态 -> 平台能力。
- 先复现再修复，记录复现条件与日志。
- 修复后补最小回归用例，避免同类问题反复出现。
- 页面初始化尽量先渲染框架结构，再异步补数据，降低白屏感知时间。
- 页面转场卡顿优先排查：`onLoad/onShow` 重逻辑、`onPageScroll` 高频样式改动、首屏静态 DOM 过大。
- 静态资源与页面跳转异常优先排查“运行时路径是否误用相对路径”。
- 组件未渲染优先排查：组件引用方式（easycom/显式导入）是否与当前写法匹配。
- 脚本导入异常优先排查：`js` 引用是否误用了 `/` 开头路径、`node_modules` 是否在项目根目录。
- 样式未生效优先排查：`@import` 是否写在 `<style>` 内、路径是否为相对路径、语句是否以 `;` 结束。
- `json` 导入异常优先排查：路径写法（相对路径或 `@/`）与别名语义（uni-app/CLI）是否一致。
- 静态资源异常优先排查：是否误把非 `static` 资源当作模板直链路径使用。
- 平台报错优先排查：是否误用浏览器全局对象（`window/document/navigator`）导致非 H5 端异常。
- 样式兼容异常优先排查：是否混用 `vue` 与 `nvue` 的 CSS 能力边界（单位、选择器、全局样式）。
- Vue 渲染异常优先排查：`v-for` 的 `key` 是否稳定、`v-if/v-show` 是否误用、事件回调是否写入复杂表达式。
- 组件通信异常优先排查：`props` 是否被子组件直接修改、`$emit` 事件名是否一致、插槽作用域是否误解。
- 生命周期异常优先排查：`onLoad/onShow/onUnload` 职责是否混用、副作用是否未清理。
- 组合式 API 异常优先排查：`setup` 中是否误用 `this`、`reactive` 解构后是否丢失响应性、`watch` 是否缺少清理。
- Vue3 迁移异常优先排查：`v-model` 新语义、`v-bind` 合并顺序、`v-if/v-for` 同节点行为是否按 Vue3 规则实现。
- Vue3 组件异常优先排查：`defineProps/defineEmits` 是否声明完整、`defineExpose` 是否暴露最小接口、插槽语义是否清晰。
- Vue3 API 异常优先排查：`toRefs` 是否漏用、`nextTick` 时机是否正确、`getCurrentInstance` 是否被滥用。
- 组合式页面钩子异常优先排查：是否误用 `onShow/onHide`，应改为 `onPageShow/onPageHide`。
- 组合式 API 导入异常优先排查：是否把 `vue` 与 `@dcloudio/uni-app` 的 API 来源混用。
- 小程序原生组件不生效优先排查：`pages.json -> usingComponents` 是否放在正确平台节点（如 `wxcomponents/mycomponents/swancomponents/ttcomponents/kscomponents/jdcomponents/xhscomponents`）。
- 小程序脚本扩展不生效优先排查：是否把 `wxs/sjs/filter` 用错平台，或误复用非本平台语法。
- 小程序专属 API 报错优先排查：是否缺少 `MP-*` 条件编译保护，导致非目标端执行。
- 多小程序平台组件冲突优先排查：组件别名是否重名、路径是否在各平台节点一致维护。
- 微信插件不生效优先排查：`manifest.json -> mp-weixin.plugins` 是否声明了正确的 `version/provider`。
- 小程序插件组件报找不到优先排查：页面 `usingComponents` 是否使用了正确协议（微信 `plugin://`、百度 `dynamicLib://`）。
- 微信插件权限报错优先排查：`manifest.json -> mp-weixin.permission` 是否补齐插件要求权限。
- 分包插件路由失败优先排查：是否违规“分包外直跳分包插件页”或同插件被多个分包同时引用。
- 插件开发构建失败优先排查：`plugin/plugin.json` 是否存在且 `main/publicComponents/pages` 路径有效。
- 插件开发产物异常优先排查：编译命令是否携带 `--plugin <插件目录名>`。
- 插件开发端差异异常优先排查：是否正确使用 `MP-WX-PLUGIN/MP-ALIPAY-PLUGIN` 条件编译宏。
- 插件发布后页面/组件缺失优先排查：是否在 `main.js` 直导入关键页面组件以防摇树优化裁剪。
- 微信分包异步化不生效优先排查：是否启用 `lazyCodeLoading: "requiredComponents"`，以及基础库是否低于 `2.11.2`。
- 微信分包异步化首屏白屏优先排查：`componentPlaceholder` 是否缺失或占位组件过重。
- 分包异步化组件异常优先排查：项目是否使用 `vue3`（该能力当前不支持 `vue3`）。
- 跨分包 JS 报模块不存在优先排查：是否误用静态/动态 `import`，应改为 `require('../sub-package/...')`。
- Vuex 异常优先排查：`main.js` 是否执行 `app.use(store)`、`nvue createApp` 是否返回 `Vuex`。
- Vuex 更新异常优先排查：是否绕过 `mutation` 直接改 `state`，或在 `mutation` 中执行异步逻辑。
- Pinia 异常优先排查：`main.js` 是否执行 `createPinia` 并 `app.use(pinia)`。
- Pinia 响应性异常优先排查：是否直接解构 store 状态而未使用 `storeToRefs`。
- Pinia 在 `nvue` 页面异常优先排查：`createApp` 返回值是否包含 `Pinia`。
- Vue3 迁移构建异常优先排查：是否仍保留 `require/module.exports`、`Vue.prototype` 等 Vue2 写法。
- 小程序迁移事件异常优先排查：是否继续使用 `.native` 或 `click`，以及是否需要 `data-eventsync`。
- 迁移后白屏优先排查：`manifest.json` 中微信小程序 `optimization.minified` 是否开启。
- TypeScript 报错优先排查：`typescript/@dcloudio/types` 是否安装，`tsconfig` 是否配置 `types: ["@dcloudio/types"]`。
- TS 组件链路异常优先排查：`<script lang="ts">` 页面在 Vue2 兼容链路下是否引用了无 TS 能力组件。
- JSX 构建异常优先排查：`vite` 是否启用 `@vitejs/plugin-vue-jsx`，插件版本是否与 `vite` 匹配。
- JSX 端兼容异常优先排查：是否把 JSX 组件用于不支持的端（如小程序）。
- 条件编译异常优先排查：是否误用平台宏、是否把 `#ifdef` 写在不支持的位置。
- 样式/模板条件编译异常优先排查：注释是否用 HTML/CSS 语法，而非 JS 注释。
- App 平台判断异常优先排查：是否在 `APP-PLUS` 条件下调用 `plus.*`，以及是否误用了 `plusready` 监听。
- H5 跨域异常优先排查：是否为浏览器同源策略拦截、服务端是否放开来源与预检请求。
- 跨域定位优先排查：当前是否处于浏览器内核链路（H5/web-view/renderjs/WKWebView）。
- 宽屏适配异常优先排查：当前是否误把等比缩放当作默认方案，是否缺少局部拉伸布局。
- `rpx` 异常优先排查：`pages.json` 的 `rpxCalc*` 参数是否与设计基准一致。
- 分栏布局异常优先排查：是否误把 `leftWindow/rightWindow/topWindow` 用在非 web 端。
- SSR 首屏异常优先排查：`manifest` 的 `h5.ssr` 是否启用、`uni-ssr` 云函数与静态资源是否同版本发布。
- SSR 数据异常优先排查：同构代码是否在服务端与客户端执行出不同结果（时间、随机数、环境分支）。
- nvue 样式异常优先排查：是否误用了 `background-image`、百分比布局、媒体查询或复杂选择器。
- nvue 文本异常优先排查：文本是否放在 `<text>` 节点，字体样式是否写在 `<text>` 上。
- nvue 显隐异常优先排查：是否误把 `v-show` 当作核心显隐手段。
- nvue 页面丢失优先排查：是否开启 `renderer:native` 后仍依赖 `vue` 页面链路。
- nvue 样式编译异常优先排查：`nvueStyleCompiler` 是否与项目策略一致（`uni-app/weex`）。
- nvue 全局样式异常优先排查：`App.vue` 样式是否影响 nvue，是否需要 `APP-PLUS-NVUE` 隔离。
- nvue 通讯异常优先排查：是否仍使用旧 `postMessage` 方案，是否改为 `uni.$on/uni.$emit` 并在卸载时 `uni.$off`。
- nvue API 不生效优先排查：是否误用了不支持 API（`uni.createAnimation/uni.pageScrollTo/uni.createIntersectionObserver`）且未替代实现。
- nvue 事件穿透异常优先排查：是否误把 `eventPenetrationEnabled/userInteractionEnabled` 当作全端能力（它们仅 iOS + `view` 生效）。
- nvue 曝光事件异常优先排查：`appear/disappear` 是否绑定在可滚动区域内元素，是否误期望 `direction` 之外字段。
- Native.js 异常优先排查：是否在非 App 端或浏览器预览环境执行了 `plus.android/plus.ios` 调用。
- Native.js 端差异异常优先排查：是否遗漏 `APP-ANDROID/APP-IOS` 分支隔离，导致类名/方法签名不匹配。
- renderjs 不生效优先排查：当前是否 `app-vue/H5`、脚本是否内联声明、`lang/module` 标记是否完整。
- renderjs 通讯异常优先排查：`callMethod` 目标方法是否定义在页面 `methods`，以及参数是否可序列化。
- App UA 异常优先排查：`manifest` 中是否配置了 `useragent`，以及 `concatenate` 是否符合后端识别预期。
- App UA 配置不生效优先排查：是否重新云端打包、是否误在预览环境验证最终 UA 行为。
- App 毛玻璃异常优先排查：当前页面是否 `app-vue`，以及是否误在 `nvue/H5/小程序` 端验证 `blurEffect`。
- App 毛玻璃样式异常优先排查：是否把 `blurEffect` 与 `rgba/gradient` 背景混用。
- 鸿蒙构建异常优先排查：`HBuilderX` 是否达到 `4.27+`，项目是否已迁移到 `vue3`。
- 鸿蒙能力接入异常优先排查：是否误沿用 Android/iOS 插件流程而非鸿蒙专题路线。
- 鸿蒙运行异常优先排查：DevEco Studio 是否可用、模拟器是否在 DevEco 中启动、API 版本是否匹配。
- 鸿蒙发行异常优先排查：证书签名配置是否完整、`debug/release` 构建模式是否选对。
- 鸿蒙三方库导入异常优先排查：是否在页面直接 `import` ohpm 包，而非通过 UTS 插件中转。
- 鸿蒙 UTS 插件不生效优先排查：`package.json` 是否配置 `uni-ext-api` 且 `app.arkts=true`。
- 鸿蒙原生组件不生效优先排查：是否满足 `HBuilderX 4.62+`，且 `ets` 文件已在 `app-harmony/index.uts` 导入。
- 鸿蒙 embed 事件异常优先排查：`embed` 事件名是否与 `defineNativeEmbed` 定义一致，`tag` 是否全小写。
- 鸿蒙地图能力异常优先排查：是否配置 `sdkConfigs.maps.qqmap.key`，以及是否在当前版本入口正确维护。
- 鸿蒙 WebView 通讯异常优先排查：是否仍依赖 `plus` 能力，未切换 `WebviewContext.evalJs` + `uni.postMessage` 桥接。
- 鸿蒙 URL Scheme 唤起异常优先排查：`module.json5` 是否以追加方式配置 `skills`，并校验 `actions/scheme/host` 约束。
- 鸿蒙 App Linking 失败优先排查：AGC 配置、`/.well-known/applinking.json`、`module.json5` 的 `https host` 是否一致。
- 鸿蒙 `canOpenURL` 异常优先排查：`module.json5` 根节点是否配置了 `querySchemes`，且待检测 scheme 在白名单内。
- 鸿蒙 Deep Linking 报错码（如 `17700056`）优先排查：是否遗漏 `querySchemes` 或传入了非法 scheme。
- 鸿蒙 App Linking 拉起异常优先排查：是否误把 `canOpenURL` 用在 `https` 链路，应直接 `openSchema`。
- 鸿蒙元服务拉起异常优先排查：是否通过 UTS 插件封装 AbilityKit，页面是否绕过封装直调原生能力。
- 鸿蒙支付异常优先排查：`uni_modules/uni-pay/config.js` 是否按端配置 `huawei.mp/mp-harmony` 与 `huawei.app/app-harmony`。
- 鸿蒙支付签名异常优先排查：`mchId/mchAuthId/mchPrivateKey/platformPublicKey` 是否来自同一商户体系且格式完整。
- 鸿蒙支付回调异常优先排查：`notifyUrl` 是否为当前项目 uniCloud 地址，且 HBuilderX 登录账号与云端一致。
- 鸿蒙应用支付入口无效优先排查：`manifest.json` 鸿蒙 `uni-payment` 是否已启用华为支付能力。

## 示例

- 页面白屏：先查 `pages.json` 注册，再查编译报错。
- 接口 401：先查 token 注入，再查登录态恢复流程。
- 白屏优化：首屏只保留必要节点，复杂模块延后加载并加骨架占位。
- 卡顿优化：把非首屏请求放到首屏渲染后，滚动事件里只做轻量状态更新。
- 图片 404：先检查 `src` 是否为 `/static/...` 绝对路径，再检查资源是否真实存在。
- 跳转失败：先检查 `url` 是否为 `/pages/...` 绝对路径，再检查 `pages.json` 注册。
- 组件不生效：先检查标签命名是否命中 easycom，再检查是否需要显式导入与注册。
- 脚本导入失败：先把 `/common/a.js` 改成 `@/common/a.js` 或相对路径，再检查别名配置。
- 第三方库报错：若提示 `window/document` 未定义，先判定该库是否仅支持 H5。
- 样式不生效：先检查 `@import "./index.css";` 写法，再检查样式文件相对路径是否正确。
- `json` 读取异常：先确认 `import` 路径，再确认目标平台条件编译后字段是否存在。
- 图片不显示：先确认资源在 `static`（可直链）还是非 `static`（需 `import` 绑定）。
- `window is not defined`：先定位调用点，再改为 `uni` API、条件编译或平台适配层实现。
- App 样式异常：先确认页面是 `vue` 还是 `nvue`，按渲染机制分别排查。
- 小程序样式丢失：先确认背景图/字体是否使用受限本地路径，再改为网络地址或端内可用方案。
- 列表显示错乱：先检查 `v-for` 的 `key` 是否使用业务唯一标识。
- 子组件状态不同步：先检查是否直接修改了 `prop`，再检查父组件是否监听对应 `$emit` 事件。
- 页面返回卡顿：先检查 `onShow` 是否重复执行重逻辑，再检查定时器/监听是否重复注册。
- 数据不更新：先检查是否对 `reactive` 直接解构使用，再改为 `toRefs` 或保持对象引用。
- 双向绑定失效：先确认组件是否声明 `modelValue` 与 `update:modelValue` 事件。
- 子组件方法不可调用：先确认是否通过 `defineExpose` 显式暴露，再检查父组件 `ref` 使用时机。
- 视图读取异常：先检查是否在渲染完成前读取节点，必要时补 `nextTick`。
- 组合式页面不触发显隐逻辑：先检查是否使用 `onPageShow/onPageHide`，避免继续沿用 `onShow/onHide`。
- 组合式 API 报导入错误：先检查 API 来源，页面生命周期从 `@dcloudio/uni-app` 导入，响应式能力从 `vue` 导入。
- 页面报 `$store` 未定义：先检查入口是否 `app.use(store)`，再检查模块导出与引入路径。
- `mapState/mapActions` 在 `nvue` 不生效：先检查 `createApp` 返回值是否包含 `Vuex`。
- Pinia 状态更新后页面不刷新：先检查状态字段是否通过 `storeToRefs` 解构。
- Options API 页面无法映射 Pinia：先检查是否使用 `mapState/mapWritableState/mapActions` 且传入 `useXxxStore`。
- 迁移后事件不触发：先把组件事件从 `.native` 改为显式 `emits`，小程序场景优先使用 `tap`。
- 页面参数为空：先检查目标页面是否通过 `props`/`defineProps` 接收参数，而非仅依赖旧式读取路径。
- Promise 错误分支失效：先检查迁移后是否仍按旧格式处理 uni API 返回值。
- TS 编译提示找不到 `uni`：先检查 `tsconfig` 的 `types` 是否包含 `@dcloudio/types`。
- TS 页面导入组件报类型异常：先检查页面与组件的 TS 链路和导入声明是否一致。
- JSX 代码不生效：先检查 `vite.config` 是否注册 `vueJsx()`，再检查 `app-vue3/h5-vue3` 端配置。
- 小程序端 JSX 页面异常：先回退到模板语法实现，避免在不支持端强行共用 JSX 组件。
- 条件编译未生效：先核对平台宏（如 `APP-PLUS/APP-ANDROID/H5/MP-WEIXIN`）再检查条件块位置。
- 样式条件编译失效：先检查是否写成 `// #ifdef`，改为 CSS/HTML 注释语法。
- App 端 `plus is not defined`：先确认是否在 `APP-PLUS` 条件分支内调用，再排查执行环境是否真机/App。
- nvue 页面调用 plus 失败：先确认是否误用 `document.addEventListener`，改为 `plus.globalEvent.addEventListener` 或直接调用 plus API。
- H5 控制台报 CORS：先确认请求来源域名，再检查服务端 `Access-Control-*`、预检 OPTIONS、网关转发。
- 浏览器插件下可用但线上失败：先移除插件复测，再改为代理或服务端 CORS 白名单方案。
- 宽屏页面元素异常放大：先检查是否全局使用 `rpx`，再核对 `rpxCalcMaxDeviceWidth/rpxCalcBaseDeviceWidth/rpxCalcIncludeWidth`。
- 分栏在 App/小程序不生效：先确认是否依赖 `rightWindow`，改为页面内 `uni-row` + 断点方案。
- iPad 横屏布局错位：先改为运行时响应式（`match-media`/媒体查询）而非新增 PC 条件编译页面。
- SSR 页面首屏正常但交互错乱：先检查 hydration 警告，再比对服务端与客户端首屏数据是否一致。
- SSR 报 `window/document is not defined`：先把浏览器对象访问移到 `onMounted` 或客户端分支。
- SSR 请求未触发：先检查是否把数据加载写在仅客户端执行的钩子中（如只放 `onMounted`）。
- nvue 文字颜色不生效：先检查文字是否在 `<text>` 内，再检查样式是否作用于 `<text>`。
- nvue 背景图不显示：先把 `background-image` 改为 `<image>` 背景层方案。
- nvue 长列表卡顿：先评估 `list/recycle-list/waterfall`，避免继续堆 `scroll-view`。
- App 打包后部分页面空白：先检查 `renderer:native` 与页面类型（`vue/nvue`）是否匹配。
- nvue 样式突变：先检查是否改了 `nvueStyleCompiler`，再核对历史页面是否依赖旧编译行为。
- nvue 公共样式异常：先确认 `App.vue` 样式是否被编译到 nvue，再用 `APP-PLUS-NVUE` 条件隔离。
- nvue 页面多次触发同一消息：先检查离开页面时是否执行 `uni.$off` 清理全局事件监听。
- nvue 页面动画/滚动能力缺失：先检查是否误用不支持的 uni API，再改为 `animation/dom` 模块能力。
- nvue 浮层点击穿透不符合预期：先确认页面是否 iOS，目标节点是否为 `view`，再检查 `eventPenetrationEnabled` 与 `userInteractionEnabled`。
- nvue 长按事件取不到时间戳：先确认当前是否 H5 端，再给 `timestamp` 增加兼容兜底字段。
- Native.js 调用报方法不存在：先确认当前平台分支，再核对原生类路径与方法签名是否与端一致。
- Native.js 调试与正式行为不一致：先确认是否在真机/自定义基座验证，再排查运行环境差异。
- renderjs 回调逻辑层无响应：先确认 `this.$ownerInstance.callMethod` 方法名与页面 `methods` 完全一致。
- renderjs 在 Vue3 页面失效：先确认是否误依赖 `script setup` 暴露的方法，改为 `methods` 承接回调。
- App 端接口 UA 未携带业务标识：先检查 `concatenate` 是否被设为 `false` 覆盖默认 UA，再核对打包版本。
- Android/iOS UA 不一致：先检查是否配置了 `useragent_android/useragent_ios` 覆盖通用配置。
- titleNView 毛玻璃不生效：先确认是否在页面样式 `titleNView.blurEffect` 配置，再核对运行端是否 `app-vue`。
- tabBar 毛玻璃不生效：先确认是否在 `pages.json.tabBar` 下配置 `blurEffect`，并检查 `HBuilderX` 版本是否满足 `3.99+`。
- 鸿蒙包无法编译：先检查 `HBuilderX` 版本，再检查项目是否为 `vue3`。
- 鸿蒙原生能力不可用：先定位能力所属专题（原生 API/组件/内置模块），再按专题文档校验接入链路。
- 鸿蒙运行时报工程异常：先检查项目路径是否含中文或过长，再执行“清空缓存并重建”。
- 鸿蒙真机安装失败：先检查调试证书/签名，再确认设备 API 版本是否满足项目要求。
- 鸿蒙页面调用三方能力失败：先确认页面仅调用 UTS 插件 API，再检查插件内部 ohpm 引用位置。
- 鸿蒙插件注册失败：先检查插件命名是否误用 `uni-` 前缀，再检查 `arkts` 配置是否开启。
- 鸿蒙原生组件配置改了但页面不更新：先检查 `options` 是否重新赋值对象，而非原地修改字段。
- WebView 叠放下组件不可见：先检查 `defineNativeEmbed` 是否按场景配置 `nodeRenderType`。
- 鸿蒙定位/地图不生效：先检查腾讯地图 key，再确认构建版本是否已包含最新 manifest 配置。
- 鸿蒙 WebView 回调页面失败：先确认 `evalJs` 目标函数已挂载，再检查页面侧 `uni.postMessage` 监听链路。
- Deep Linking 点击无反应：先检查 `skills` 中是否误覆盖 `skills[0]`，再核对 `scheme://host` 是否与配置完全一致。
- App Linking 仅打开浏览器不拉起应用：先检查 `uris.scheme` 是否为 `https`、`host` 是否匹配 AGC 与 `applinking.json`。
- 鸿蒙拉起参数为空：先区分冷启动与热启动回调路径，再用 `uni.getLaunchOptionsSync` 复核参数读取。
- Deep Linking `canOpenURL` 一直返回 false：先检查 `querySchemes` 是否配置到 `module.json5` 根节点，再核对 scheme 是否完全匹配。
- Deep Linking 抛 `17700056`：先检查是否缺少 `querySchemes`，再核对调用参数格式与白名单数量限制（最多 50）。
- App Linking 调用无效：先去掉 `canOpenURL` 预检，直接调用 `openSchema(https://...)` 验证链路。
- 元服务无法唤起：先检查 UTS 插件是否已暴露对应方法，再核对插件内 AbilityKit 配置与参数映射。
- 鸿蒙元服务支付报参数错误：先核对是否使用 `huawei.mp` 且 `clientType=mp-harmony`。
- 鸿蒙应用支付不出现渠道：先核对 `huawei.app` 配置与 `manifest` 的 `uni-payment` 华为支付开关。
- 支付签名校验失败：先重新核对 `mchAuthId` 与商户私钥是否匹配，再确认平台公钥为证书正文内容。
- 支付完成但业务侧未回调：先核对 `notifyUrl` 与 uniCloud 项目地址，再检查账号一致性与服务端日志。
- 小程序原生组件加载失败：先检查 `usingComponents` 是否写在目标平台键下，再核对组件路径是否可在该端解析。
- 小程序模板计算不生效：先确认微信使用 `wxs`、支付宝/百度使用 `sjs`、QQ/字节按 `filter` 实现。
- 小程序端 API 在其他端报错：先补 `#ifdef MP-*` 条件编译，再为非目标端增加降级提示。
- 多平台小程序同页组件异常：先排查组件别名冲突，再排查各平台节点映射是否错位。
- 微信插件组件不显示：先检查 `manifest mp-weixin.plugins` 声明，再核对 `usingComponents` 的 `plugin://` 路径。
- 百度动态库组件不生效：先确认是 `dynamicLib` 声明，再核对 `dynamicLib://` 组件路径。
- 微信插件调用报权限不足：先补 `mp-weixin.permission`，再重新编译真机验证授权流程。
- 微信插件提示版本不存在：先去微信插件后台确认可用版本，再同步更新 `manifest` 版本号。
- 分包插件页无法打开：先跳转到该分包普通页，再在分包内触发插件页跳转。
- 插件开发编译产物缺页：先检查 `plugin/plugin.json` 的 `pages`，再核对是否按 `--plugin` 模式编译。
- 插件组件在开发版可见但发布后消失：先将组件与页面在 `main.js` 显式导入，再重新发布验证。
- 插件代码误在宿主侧执行：先核对 `MP-WX-PLUGIN/MP-ALIPAY-PLUGIN` 条件编译块范围。
- 微信插件页面打不开且基础库过低：先升级到 `1.9.6+`（含插件页需 `2.1.0+`）再复测。
- 微信分包异步化未触发：先核对基础库版本，再确认 `lazyCodeLoading` 写在 `manifest -> mp-weixin`。
- 分包异步化页面卡首屏：先补 `componentPlaceholder`，再将占位组件替换为极简骨架。
- 分包异步化项目编译后组件异常：先确认是否处于 `vue3` 项目链路，再评估回退或改造方案。
- 跨分包 JS 调用失败：先把 `import` 改为 `require('../sub-package/...')`，并验证路径以调用文件为基准。

## 反例

- 未定位根因直接改代码，导致症状迁移。
- 只在一个端验证就关闭问题单。
- 进入页面立即执行多段同步计算，阻塞首屏渲染。
- 在 `onPageScroll` 中频繁改动布局与样式，导致滑动掉帧。
- 在组件内用 `../static/a.png` 动态拼接路径，迁移页面后大量资源失效。
- Options API 页面只 `import` 不注册组件，运行时出现未定义告警。
- 在 uni-app 项目中把 `node_modules` 放进子目录，导致构建和运行时模块找不到。
- 在样式里写 `@import "/styles/base.css"` 或省略分号，导致编译或加载失败。
- 在 CLI 项目中错误理解 `@` 指向，导入 `json` 时频繁报模块不存在。
- 在模板中直接引用非 `static` 路径，某些端构建后资源丢失。
- 在小程序/App 代码里直接访问 `window/document`，上线后主流程报错。
- 在 `nvue` 页面使用 `vh/vw/rem` 或 web 选择器特性，导致布局与样式异常。
- 列表项使用随机值或索引作为 `key`，导致局部更新后状态错位。
- 子组件直接改 `prop` 或依赖 `.native` 事件做跨端统一处理，导致行为不稳定。
- 页面卸载后未清理定时器或订阅，导致多次进入后逻辑叠加。
- 在 `setup` 中访问 `this` 或滥用 `watchEffect` 重请求，导致运行时异常与性能抖动。
- 按 Vue2 习惯处理 `v-model` 或 `v-bind` 覆盖顺序，导致 Vue3 端行为异常。
- Vue3 组件未声明 `emits` 或滥用多级 props 透传，导致契约不清晰与调试困难。
- 直接解构 `reactive` 字段且未使用 `toRefs`，导致状态更新不生效。
- 在组合式页面里继续用 `onShow/onHide` 监听页面显隐，导致钩子不触发或语义混乱。
- 把页面生命周期 API 从 `vue` 导入，或把响应式 API 从 `@dcloudio/uni-app` 导入，导致运行时/类型异常。
- 在组件中直接改 `store.state` 或在 `mutation` 里写异步请求，导致状态流不可追踪。
- Vuex 模块不开 `namespaced` 却跨模块同名提交，导致状态和调用混乱。
- 直接从 Pinia store 解构状态字段并在模板使用，导致响应性丢失。
- 只在 `setup` 注入 Pinia 但未在入口 `app.use(pinia)`，导致页面拿不到 store。
- Vue3 项目继续混用 `module.exports` 与 `import`，导致运行时模块解析异常。
- 迁移后继续在模板使用过滤器（`{{ val | f }}`），导致编译错误。
- 微信小程序真机白屏但忽略 `optimization.minified` 配置，导致发布后不可用。
- 未安装 `@dcloudio/types` 就直接启用 `lang="ts"`，导致页面 API 类型全面报错。
- TS 代码大量使用 `any` 绕过校验，导致问题延后到运行期暴露。
- 在 uni-app 小程序链路中将 JSX 作为主组件语法，导致编译/运行不兼容。
- 升级 `vite` 后不升级 `@vitejs/plugin-vue-jsx`，导致 JSX 构建失败。
- 在 `template/style` 中继续使用 JS 注释写条件编译，导致块被当普通注释处理。
- 在 `static` 文件内容中写 `#ifdef` 期望按端裁剪，导致资源策略失效。
- 未判断平台直接调用 `plus.os.name`，导致 H5/小程序运行时报错。
- 把 H5 跨域问题归因到小程序/App `uni.request` 限制，导致修复方向偏离。
- 仅在前端改请求头“绕过跨域”，忽略服务端预检与来源校验。
- 把 `750rpx` 长期当成“100% 宽度”，导致宽屏下布局比例异常。
- 依赖 web 窗体（`left/right/topWindow`）实现全端分栏，导致非 web 端失效。
- 为适配平板直接复制 PC 条件编译页面，导致代码分叉和回归成本暴增。
- SSR 页面中直接使用 `Math.random()/new Date()` 参与首屏渲染，导致 hydration 不一致。
- 服务端渲染代码里直接访问浏览器 API，导致首屏渲染失败。
- SSR 发布只更新前端托管资源，不更新 `uni-ssr` 云函数。
- 在 nvue 中沿用 web CSS 能力（百分比、背景图、媒体查询）并假设结果一致。
- 在 nvue 里把正文写在 `view` 上并设置字体样式，导致文本样式失效。
- 为追求性能直接开启 `renderer:native`，但未改造 `vue` 页面链路。
- 在 nvue 中直接 `@import` 在线 CSS 地址，导致样式加载失败。
- 未评估就切换 `nvueStyleCompiler` 模式，导致存量样式回归异常。
- nvue 页面继续依赖 `uni.postMessage/plus.webview.postMessageToUniNView` 做主链路通讯，导致维护复杂。
- nvue 页面注册全局监听后不清理，导致重复触发与内存累积。
- 在 nvue 中直接调用 `uni.createAnimation/uni.pageScrollTo/uni.createIntersectionObserver`，导致能力失效。
- 在 Android/H5 端依赖 `eventPenetrationEnabled` 修复事件穿透，导致功能不可用。
- 把 `appear/disappear` 绑定在非滚动容器或无滚动场景，导致曝光事件长期不触发。
- 在 H5/小程序端直接调用 `plus.android/plus.ios`，导致运行期异常。
- 在未拆分平台分支前直接复用同一套 `Native.js` 类名/方法签名，导致跨端调用失败。
- 在小程序或 `nvue` 页面继续接入 `renderjs` 作为主方案，导致能力无效。
- 把业务主链路（请求/状态管理）放进 `renderjs`，导致层间耦合和排障复杂度上升。
- 通过 `src` 外链方式加载 `renderjs` 脚本，导致模块初始化失败。
- 未评估依赖就完全替换默认 UA，导致服务端设备识别与风控链路异常。
- 修改 `manifest` 的 UA 配置后不重打包直接验收，导致误判配置失败。
- 在 `nvue`/H5/小程序环境排查 `blurEffect`，导致定位方向错误。
- 在 `blurEffect` 视觉方案里继续使用 `rgba/gradient` 背景并假设可兼容，导致效果异常。
- 在未迁移 `vue3` 前直接推进鸿蒙编译验证，导致结论失真。
- 鸿蒙能力问题直接按 Android/iOS 插件排障，导致修复方向偏离。
- 忽略 DevEco 版本与模拟器 API 限制直接运行鸿蒙，导致验证结果不可用。
- 鸿蒙构建异常后反复增量编译而不清缓存，导致问题持续放大。
- 在页面层直接接入 ohpm 三方库，导致构建链路与职责边界失控。
- 鸿蒙 UTS 插件缺失 `app.arkts=true` 仍继续联调，导致能力长期不可用。
- 使用驼峰 `tag` 或未在 `index.uts` 导入 `ets`，仍继续排查页面样式问题，导致方向偏离。
- 在 `options` 对象上做原地修改并期待 `embed` 自动更新，导致状态不同步。
- 鸿蒙端沿用 `plus.webview` 通讯代码而不迁移，导致通信链路全部失效。
- 地图能力接入时跳过 key 与 manifest 校验直接调接口，导致误判为运行时 bug。
- 鸿蒙 URL Scheme 配置直接覆盖 `skills` 数组或放宽 `host=*`，导致拉起行为不可控。
- App Linking 场景忽略 `applinking.json` 与 AGC 一致性检查，导致线上唤起随机失败。
- 把 `querySchemes` 错配到 `skills` 节点或未配置就调用 `canOpenURL`，导致 Deep Linking 前置检测失效。
- 在 App Linking 场景沿用 `canOpenURL` 作为是否拉起依据，导致流程被误拦截。
- 页面层直接调用鸿蒙原生拉起 API，不走 UTS 插件封装，导致跨端维护成本失控。
- 将 `mp-harmony` 与 `app-harmony` 的支付 `clientType` 混用，导致渠道初始化失败。
- 在前端仓库存储或提交 `mchPrivateKey` 明文，导致密钥泄露与风控风险。
- 未开启鸿蒙 `uni-payment` 华为支付配置就直接验收支付流程，导致结论失真。
- `notifyUrl` 未指向当前 uniCloud 项目仍长期联调，导致回调问题被误判为 SDK 缺陷。
- 在支付宝/百度/QQ/字节小程序中直接复用微信 `wxs` 文件，导致脚本逻辑失效。
- 只在 `wxcomponents` 配置组件却在多平台复用页面，导致非微信端组件缺失。
- 小程序专属 API 不做条件编译直接运行，导致 H5/App 报未定义异常。
- 多平台 `usingComponents` 使用同名别名映射不同组件，导致行为不可预测。
- 微信插件只在页面 `usingComponents` 配置 `plugin://`，但未在 `manifest` 声明插件，导致运行失败。
- 把百度动态库按微信插件方式接入（`plugins + plugin://`），导致百度端无法加载。
- 同一微信插件同时声明在多个分包，导致分包插件冲突。
- 依赖官方文档里已下线示例版本不做校验，导致版本长期不可用。
- 插件需要权限只在微信后台配置，不在 `mp-weixin.permission` 落地，导致真机仍报错。
- 插件项目缺少 `plugin/plugin.json` 或入口配置错误仍直接发布，导致插件不可用。
- 插件调试/构建不带 `--plugin` 参数，导致误打普通小程序包。
- 插件与宿主共用同一逻辑文件却未做宏隔离，导致行为串线。
- 发布前未做关键页面组件保留导入，导致插件产物被摇树裁剪。
- 微信分包异步化场景未启用 `lazyCodeLoading` 就直接验证分包收益，导致结论失真。
- 分包异步化页面不配置 `componentPlaceholder` 或占位组件与真实组件同体量，导致首屏无收益。
- 在分包异步化能力链路中直接采用 `vue3` 并忽略官方限制，导致线上行为不可控。
- 跨分包 JS 继续使用 `import/import()`，导致运行时模块解析失败。

## 检查项

- 是否有清晰复现步骤与结论。
- 根因是否被确认并可解释。
- 是否有回归验证结果与风险说明。
- 首屏渲染时长是否可接受，是否存在明显白屏。
- 页面切换与滚动是否有掉帧或卡顿。
- 动态资源路径和跳转路径是否统一为运行时绝对路径。
- 组件引用方式是否与页面写法一致且可命中（easycom 或显式导入注册）。
- `js` 模块路径与第三方依赖位置是否符合规范。
- 样式 `@import` 写法是否符合规范（位置/路径/分号）。
- `json` 路径与字段在目标端是否可用（含条件编译差异）。
- 资源路径是否符合“`static` 直链、非 `static` import 后绑定”规则。
- 是否存在浏览器全局对象误用，及 `vue/nvue` 渲染机制误判问题。
- 是否存在 `vue/nvue` 样式能力混用、及小程序资源路径受限问题。
- 是否存在 Vue 基础指令误用导致的渲染与交互异常。
- 组件通信与插槽语义是否符合单向数据流和作用域规则。
- 生命周期与实例 API 使用是否符合页面阶段与平台能力边界。
- 组合式 API 是否正确处理响应性边界与副作用清理。
- Vue3 语义变更点是否已逐项校验并完成回归。
- Vue3 组件接口（props/emits/expose/slots）是否有清晰边界并可回归验证。
- Vue3 API 时机与响应性处理（`toRefs/nextTick/getCurrentInstance`）是否符合规范。
- 组合式页面显隐监听是否使用 `onPageShow/onPageHide` 而非 `onShow/onHide`。
- Vue3 组合式 API 导入来源是否清晰区分 `vue` 与 `@dcloudio/uni-app`。
- Vuex 注入链路是否完整（`app.use(store)`、`nvue` 返回 `Vuex`）。
- Vuex 是否保持“异步 action、同步 mutation”的约束。
- Pinia 注入链路是否完整（`createPinia`、`app.use(pinia)`、`nvue` 返回 `Pinia`）。
- Pinia 状态消费是否通过 `storeToRefs` 或保持 store 原引用访问。
- Vue3 模块规范与全局注入方式是否完成替换（ESM / `app.config.globalProperties`）。
- 小程序事件体系是否完成迁移（`.native` 下线、`tap`/`data-eventsync` 校验）。
- 迁移后 uni API Promise 语义是否已按 `then/catch` 路径回归验证。
- TypeScript 依赖、`tsconfig`、页面脚本 `lang="ts"` 三者是否配置一致。
- 请求层、状态层是否存在大量 `any` 导致的类型逃逸。
- JSX 配置是否完整（`vueJsx` 插件注册 + 版本匹配）。
- JSX 是否仅用于 `App-vue3/H5-vue3` 支持端。
- 条件编译宏与位置是否正确（`js/vue/nvue/css/pages.json/manifest/uts`）。
- 模板和样式中的条件编译注释语法是否正确。
- App 端 `plus` 能力调用是否已做 `APP-PLUS` 条件保护，且未引入 `plusready` 监听依赖。
- App 原生事件监听是否统一为 `plus.globalEvent.addEventListener`。
- H5 跨域链路是否完成服务端 CORS 与预检回归验证。
- 当前请求链路是否明确区分“浏览器受限”与“非浏览器链路”。
- 宽屏场景是否明确“局部拉伸 vs 等比缩放”策略。
- `rpxCalc*` 参数与断点布局是否已在常见宽度档位回归。
- web 专有窗体能力是否被限制在 web 端使用。
- SSR 配置、`uni-ssr` 云函数与静态资源是否保持同版本发布。
- 首屏同构数据是否可复现且不依赖随机/时序差异。
- 浏览器 API 是否已隔离在客户端执行路径。
- nvue 是否遵循受限样式边界（`text` 节点、`flex` 布局、无背景图/百分比/媒体查询）。
- 性能热点页面是否评估并使用 nvue 高性能滚动组件（`list/recycle-list/waterfall`）。
- `renderer` 与页面类型是否一致，避免 App 端页面丢失。
- `nvueStyleCompiler` 与 `App.vue` 全局样式对 nvue 的影响是否已验证。
- nvue 与 vue 通讯是否统一为 `uni.$on/$emit`，且页面卸载时完成 `uni.$off`。
- nvue 不支持 API 是否已替代并完成真机验证（`animation/dom` 等）。
- nvue 事件能力是否区分 iOS 专属属性与通用事件，且关键逻辑有端降级处理。
- `longpress/appear/disappear` 的 `timestamp/direction` 字段是否在目标端完成兼容验证。
- Native.js 是否仅用于遗留能力且已评估插件/UTS替代路径。
- Native.js 是否完成 `APP-ANDROID/APP-IOS` 分支与真机验证，不依赖浏览器预览结论。
- renderjs 是否仅用于 `app-vue/H5`，并在非支持端有明确降级方案。
- renderjs 通信链路（`props/change` 与 `callMethod`）是否完成回归，且回调方法位于 `methods`。
- renderjs 是否避免高频跨层同步，关键链路已做节流或批量更新。
- UA 配置是否明确 `concatenate` 策略，并验证后端依赖字段完整。
- Android/iOS 专属 UA 覆盖是否完成双端真机回归。
- UA 修改是否通过云端打包发布验证，而非仅本地预览验证。
- `blurEffect` 是否仅在 `app-vue` 验证，且 `titleNView/tabBar` 配置入口正确。
- 毛玻璃样式是否规避 `rgba/gradient` 并在 Android/iOS 真机完成回归。
- tabBar 毛玻璃是否验证 `HBuilderX 3.99+` 版本门槛与暗黑模式行为。
- 鸿蒙发布链路是否满足 `HBuilderX 4.27+` 与 `vue3` 条件。
- 鸿蒙能力问题是否按专题路线定位（运行发行/原生 API/原生组件/内置模块）。
- 鸿蒙运行环境是否完成 DevEco 与 API 版本校验（模拟器/真机）。
- 鸿蒙签名与证书是否在目标构建模式（`debug/release`）下完成验证。
- 鸿蒙构建异常是否执行过“清空缓存并重建”的回归验证。
- 鸿蒙三方库是否通过 UTS 插件中转，未在页面层直接 `import` ohpm。
- 鸿蒙 UTS 插件是否完成 `uni-ext-api` 与 `app.arkts=true` 配置校验。
- 鸿蒙原生组件链路是否满足：`ets` 定义 -> `index.uts` 导入 -> `embed` 小写 `tag` 调用。
- `embed options` 更新是否采用重新赋值策略，且事件名与原生定义一致。
- WebView 叠放场景是否验证 `nodeRenderType` 配置效果。
- 鸿蒙地图能力是否完成腾讯地图 key 与 manifest 配置回归。
- 鸿蒙 WebView 通讯是否完成 `evalJs` 回调和 `uni.postMessage` 监听双向验证。
- 鸿蒙 URL Scheme 是否验证 Deep Linking/App Linking 两条链路，且 `skills` 配置未破坏原有条目。
- App Linking 是否完成 AGC、`applinking.json`、`https host` 三端一致性回归。
- 拉起参数读取是否覆盖冷启动与热启动场景，并校验空参数兜底逻辑。
- `querySchemes` 是否配置在 `module.json5` 根节点，且 Deep Linking 检测 scheme 均在白名单内。
- Deep Linking 是否覆盖 `canOpenURL` 失败分支（含错误码 `17700056`）并给出可见兜底提示。
- App Linking 是否直接走 `openSchema` 链路，未混入 `canOpenURL` 误判分支。
- 元服务拉起是否全部经由 UTS 插件接口调用，页面无原生 API 直连。
- 鸿蒙支付是否按端验证 `huawei.mp/mp-harmony` 与 `huawei.app/app-harmony` 两条配置链路。
- `mchId/mchAuthId/mchPrivateKey/platformPublicKey` 是否完成来源一致性与格式校验。
- `notifyUrl` 与 uniCloud 项目地址、HBuilderX 登录账号是否完成一致性验证。
- 鸿蒙应用支付链路是否验证 `manifest` 中 `uni-payment` 华为支付开关。
- `usingComponents` 是否按目标小程序平台拆分配置，并在真机端验证可解析。
- `wxs/sjs/filter` 是否按平台边界使用，且未跨平台混用。
- 小程序专属 API 是否全部包裹 `MP-*` 条件编译并覆盖非目标端兜底。
- 多平台组件别名与路径映射是否无冲突且可回归。
- 插件声明是否在 `manifest` 对应平台节点正确配置（微信/支付宝 `plugins`，百度 `dynamicLib`）。
- 插件组件协议路径是否正确（微信 `plugin://`、百度 `dynamicLib://`），且页面已真机回归。
- 微信插件权限是否在 `mp-weixin.permission` 完整声明并通过授权验证。
- 分包插件是否满足“单插件单分包引用”与“分包外不可直跳插件页”限制。
- 插件版本号是否为后台当前可用版本且完成发布前验真。
- 插件工程是否存在有效 `plugin/plugin.json`，且入口/页面/组件路径可解析。
- 插件编译与构建命令是否统一使用 `--plugin <插件目录名>`。
- 插件与宿主代码是否通过 `MP-WX-PLUGIN/MP-ALIPAY-PLUGIN` 条件编译隔离。
- 插件关键页面与组件是否完成 `main.js` 显式导入保留并真机回归。
- 微信分包异步化是否完成 `lazyCodeLoading` 配置与基础库 `2.11.2+` 验证。
- 分包异步化页面是否为跨分包组件配置了 `componentPlaceholder` 且占位组件足够轻量。
- 分包异步化工程是否明确 `vue3` 不支持约束并完成风险标注。
- 跨分包 JS 是否只保留 `require('../sub-package/...')` 写法并完成真机回归。

## 来源

- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/page.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/page-path.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/page-component.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/page-script.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/page-style.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/page-json.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/page-static-assets.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/syntax-js.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/syntax-css.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/vue-basics.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/vue-components.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/vue-api.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/vue-composition-api.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/vue3-basics.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/vue3-components.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/vue3-api.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/vue3-composition-api.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/vue3-vuex.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/vue3-pinia.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/migration-to-vue3.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/typescript-subject.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/syntax-jsx.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/nvue-api.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/nvue-event.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/use-html5plus.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/native-js.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/renderjs.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/app-useragent.html
- 官方教程（2026-02-12 查阅）：https://uniapp.dcloud.net.cn/tutorial/app-blureffect.html
- 官方教程（2026-02-12 查阅）：https://uniapp.dcloud.net.cn/tutorial/harmony/intro.html
- 官方教程（2026-02-12 查阅）：https://uniapp.dcloud.net.cn/tutorial/harmony/runbuild.html
- 官方教程（2026-02-12 查阅）：https://uniapp.dcloud.net.cn/tutorial/harmony/native-component.html
- 官方教程（2026-02-12 查阅）：https://uniapp.dcloud.net.cn/tutorial/harmony/built-in-module.html
- 官方教程（2026-02-12 查阅）：https://uniapp.dcloud.net.cn/tutorial/harmony/url-scheme.html
- 官方教程（2026-02-12 查阅）：https://uniapp.dcloud.net.cn/tutorial/harmony/open-app.html
- 官方教程（2026-02-12 查阅）：https://uniapp.dcloud.net.cn/tutorial/mp-harmony/payment.html
- 官方教程（2026-02-12 查阅）：https://uniapp.dcloud.net.cn/tutorial/miniprogram-subject.html
- 官方教程（2026-02-12 查阅）：https://uniapp.dcloud.net.cn/tutorial/miniprogram-subcontract-asynchrony.html
- 官方教程（2026-02-12 查阅）：https://uniapp.dcloud.net.cn/tutorial/mp-weixin-plugin.html
- 官方教程（2026-02-12 查阅）：https://uniapp.dcloud.net.cn/tutorial/mp-weixin-plugin-dev.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/platform.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/CORS.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/adapt.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/ssr.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/nvue-outline.html
- 官方教程（2026-02-11 查阅）：https://uniapp.dcloud.net.cn/tutorial/nvue-css.html
