# 业务切片索引

## 使用方式

- 按模块创建业务切片，建议一个模块一组文档。
- 先填业务模型，再填接口契约，再填流程，最后填验收。
- 每次改业务逻辑时，同步更新对应模块切片。

## 推荐填写顺序

1. `01-业务模型模板.md`
2. `02-接口契约模板.md`
3. `03-业务流程模板.md`
4. `04-业务验收模板.md`

## 模块命名建议

- 文件前缀：`<模块名>-业务模型.md`、`<模块名>-接口契约.md`。
- 模块名使用业务语义：`用户`、`订单`、`预约`、`课程`。
- 规则保持“单一事实来源”，避免同一规则重复写在多个模块文件。
